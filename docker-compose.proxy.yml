services:
  supla-cloud:
    ports:
      - "${PORT_HTTP}:80"
    environment:
      - SUPLA_PROTOCOL=http
      - SUPLA_URL=https://${CLOUD_DOMAIN}

networks:
  proxy:
    external: true
    name: ${DOCKER_NETWORK_NAME:-webproxy}
