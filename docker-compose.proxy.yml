services:
  supla-cloud:
    networks: [default, proxy]
    environment:
      - SUPLA_PROTOCOL=http
      - SUPLA_URL=https://${SUPLA_HOST_ADDRESS}

networks:
  proxy:
    external: true
    name: ${DOCKER_NETWORK_NAME:-webproxy}
